// @flow
import type { Test } from './index.js.flow';

const test: Test = {
  adult: true,
  data: 'test',
  name: 'test',
  age: 11
};

console.log('=========================start==========================');
console.log(test.data, test.data);
